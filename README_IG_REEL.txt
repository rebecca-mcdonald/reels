This is the FIXED version of the IG Reel Toolkit.
The only change: correct import for AudioFileClip.

USAGE same as before:
python make_ig_reel.py --video new.mov --audio jazz.mp3 --out ig_reel_final.mp4
